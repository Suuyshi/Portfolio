// Single source of truth for basePath - used for GitHub Pages project sites
// Used by next.config.js and all components for asset paths
const basePath = "/Portfolio";
module.exports = { basePath };
